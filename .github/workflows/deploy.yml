name: Deploy Static Site

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print Hello
        run: |
          echo "===================="
          echo "🚀 Deploying to production..."
          echo "This confirms the workflow runs correctly!"
          echo "===================="

